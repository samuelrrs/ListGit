import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  container: {
    maxWidth: "700px",
    padding: "30px",
    marginTop: "25px",
    backgroundColor: "#fff",
    borderRadius: "25px",
    opacity: '1',

  },
}));

export default useStyles;
