import { makeStyles } from '@material-ui/core/styles';


const useStyles = makeStyles(() => ({
    footer: {
        padding: '1rem',
        color: 'white',
        position: 'fixed',
        bottom: '0',
    },
  }));

export default useStyles;